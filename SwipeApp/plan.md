# План внедрения фичи: окно "Это взаимно" и переход в чат

## Этапы
1. Анализ существующей логики свайпа и навигации — Готово
2. Создание мок-экрана чата `ChatScreen` — Готово
3. Добавление маршрута `Chat` в навигатор — Готово
4. Изменение обработчика лайка: при рандомном мэтче показывать окно "Это взаимно" с предложением перейти в чат — Готово
5. Тестирование сценария мэтча и перехода в чат — В процессе

## Детали реализации
- Файл `src/screens/SwipeScreen.tsx`: при свайпе вправо с вероятностью 50% показывается `Alert` с кнопкой "Написать", которая переводит на экран `Chat` и передаёт выбранного пользователя.
- Файл `src/screens/ChatScreen.tsx`: моковый интерфейс чата с заголовком и несколькими пузырями сообщений.
- Файл `src/navigation/AppNavigator.tsx`: добавлен маршрут `Chat` в стек авторизованного приложения.
- Файл `src/screens/index.ts`: экспортирован `ChatScreen`.

## Следующие шаги
- [ ] Доработать UI окна мэтча до кастомного модального экрана (по желанию)
- [ ] Автоматический переход в чат без подтверждения (если потребуется)
- [ ] Интеграция с реальными данными чатов (после появления backend)
